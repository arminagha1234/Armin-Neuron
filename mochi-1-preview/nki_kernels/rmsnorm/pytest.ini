[pytest]
# Treat this directory as the rootdir so collection stays self-contained and
# does not walk up into unrelated parent packages. importlib import mode lets
# the test import rmsnorm_ref / rmsnorm_nki as top-level modules.
addopts = --import-mode=importlib --confcutdir=.
