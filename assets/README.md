# assets

Drop the two README images here (exact filenames):

- `trainium.jpg` — a Trainium2 board / chip photo. Your own photo, or an
  official AWS press image **with attribution** (AWS Newsroom / re:Invent
  media kit). Don't commit images you don't have rights to.
- `armin-chip.jpg` — the fab / chip selfie.

PNG works too — just change the extension in the `<img>` tags in the
top-level `README.md`.
