# Gemma 4 E4B — Native PyTorch (stub)

See prior PR #8 for the native PyTorch path (eager mode, TP=2, torch.compile).

This subfolder is a placeholder. The active development path is `../vllm-neuron/`.
